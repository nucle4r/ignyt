{
  "name": "ignyt-pub",
  "version": "1.0.0",
  "description": "A package designed for reserving tables and browsing the menu at Ignyt Pub",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "prebuild": "cd backend && npm install & cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "postbuild": "mv -f frontend/build backend/src/public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18 <19"
  }
}
